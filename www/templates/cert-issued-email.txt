Greetings, {{ fullname }}!

Your ICN tutorial chat certificate has been approved and is ready for installation.  

You can see the base64 encoded cert as below

{{ cert_base64 }}

, and copy the string into the textarea in "install signed certificate" dialog; the dialog can be accessed by clicking the tools button on the top right corner of the page, then click the "install signed certificate" button.

Sincerely, 
Zhehao
